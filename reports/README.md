# Reports are saved here as JSON files after each coaching session ends.
# Each file is named <session_id>.json and contains:
#   - summary_scores (overall, clarity, structure, confidence, engagement)
#   - trends (improving/stable/declining per dimension)
#   - strengths and improvement_areas
#   - score_timeline (per 30s snapshot)
#   - raw_snapshots (full data for each 30s window)
